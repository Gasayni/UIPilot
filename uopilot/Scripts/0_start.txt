                                    // 0. Гаджи. ПочтаРоссии. Нулевой скрипт



// включаем поиск и пишем слово "Флаг"
// от него переходим к "!" (CTRL+DOWN)
// забираем логин и пароль в переменные (F2)
// переставляю "!" на ячейку ниже (CTRL+С, CTRL+V)
// сохраняю и закрываю эту таблицу
// испльзую этот логин 3 раза, если нужно еще, то повторяю эти действия



// открываю таблицу с заказами
// включаем поиск и пишем слово "Флаг"
// от него переходим к "!" (CTRL+DOWN)
// переходим к отправителю и забираем текст
// разбиваем текст на составляющие и забираем в переменные
// аналогично с получателем
// аналогично с товаром

// Потом, когда полностью оформится заказ, переходим к трек-номеру и вставляем полученный
// затем передвигаем флаг "!" вниз на одну




         // а дальше работа с сайтом

// заходим в Edge
// в поле адреса вводим "pochta.ru"
// заходим на сайт
// заходим отправить --> посылку

                // заполняем параматеры доставки
// откуда(адрес), предварительно очистив поле, выбираем первый из выпадающего списка
// куда(страна), выбираем первый из выпадающего списка
// вес посылки(в кг. с запятой), выбираем первый из выпадающего списка
// выбираем ускоренный (стрелкой вправо)
// и нажимаем "перейти к оформлению"

                // заполняем данные отправителя
// отправитель(ФИО, латинскими), предварительно очистив поле
// адрес отправителя, предварительно очистив поле, выбираем первый из выпадающего списка
// телефон отправителя(в формате +7), , предварительно очистив поле
// Email отправителя(при наличии)

                // заполняем данные получателя
// отправитель(ФИО, латинскими), предварительно очистив поле
   // если данные введены кириллицей (проверить после ввода и перехода на следующее поле,
   // если есть текст "Необходимо заполнить латинскими буквами" переходим на предложенный сайтом вариант)
// адрес отправителя, предварительно очистив поле, выбираем первый из выпадающего списка
// телефон отправителя(предварительно оставляем последние 10 цифр и при вставке добавляем соответствующий префикс), предварительно очистив поле

                // заполняем данные о товаре
// в категории отправления выбираем из выпадающего списка "Продажа товаров" (!!! рекомендуется Проверка)
// в валюта выбираем из выпдающего списка (в зависимости от страны нужную валюту) (!!! рекомендуется Проверка)
// наименование предмета
// количество
// общий вес
// общая стоимость
// код ТНВЭД
// страна происхождения (из выпадающего списка пишем букву R затем сдвигаем на 1 ниже)   (!!! рекомендуется Проверка, но как?)

// способ оплаты -- "в отделении"
// нажать "получить трек-номер"

// на след странице ввести в поиске "RR" перейти к 1 совпадению,
// выделить весь текст этого совпадения, скопировать в буфер обмена и присвоить это значение переменной

// перейти снова к заказу, вставить значение трек-номер в соответствующую ячейку и передвигаем флаг "!" вниз на одну



// загружаем следующий скрипт
load_script 1 1_Take_Logins.txt
// запустить скрипт в 1 вкладке
start_script 1



hint Скрипт 0 закончил свою работу
end_script
